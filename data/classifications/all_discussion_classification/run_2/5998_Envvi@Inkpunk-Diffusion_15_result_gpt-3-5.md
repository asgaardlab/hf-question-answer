## https://huggingface.co/Envvi/Inkpunk-Diffusion/discussions/15

contains_question: yes  
question_part: Sorry for the dumb question, but if I used `Envvi/Inkpunk-Diffusion` as the $MODEL_NAME (not the ckpt filename, just the folder path), did it pick up `Inkpunk-Diffusion-v2.ckpt` to use and not the v1 ckpt which is also in that folder? 