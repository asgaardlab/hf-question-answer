## https://huggingface.co/ali-vilab/MS-Image2Video/discussions/9

contains_question: yes
question_part: Can this be used with just a text input instead of an image input? it seems like it might just be using CLIP as input, since the output video doesn't start with the image as a frame, so could we just pass a prompt as an input?