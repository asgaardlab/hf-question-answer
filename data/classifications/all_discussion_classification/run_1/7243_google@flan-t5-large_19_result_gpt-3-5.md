## https://huggingface.co/google/flan-t5-large/discussions/19

contains_question: yes

question_part: Why is T5_large loaded for training, e.g. outputs = model(input_ids, labels = target_ids) and the output outputs.loss is NAN. But on the same dataset loaded T5_small model outputs.loss is correct.